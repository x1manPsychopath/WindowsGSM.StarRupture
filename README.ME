# WindowsGSM.StarRupture

WindowsGSM plugin for running a **StarRupture Dedicated Server** with one click.

This plugin wraps the official dedicated server executable:

```bash
StarRuptureServerEOS.exe -Log -port=7777